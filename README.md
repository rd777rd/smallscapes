A website for a small landscaping business based in Indiana that specializes in paver patios and outdoor living areas.
## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` to your Gemini API key
3. Run the app:
   `npm run dev`
